package main

import "testing"

func TestHello(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}

func TestHello_2(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}

func TestHello_3(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}

func TestHello_4(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}

func TestHello_5(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}

func TestHello_6(t *testing.T) {
    want := "Hello, World!"
    if got := hello(); got != want {
        t.Errorf("hello() = %q, want %q", got, want)
    }
}